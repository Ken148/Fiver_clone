<!DOCTYPE html>
<html>
  <head>
    <title>All Posts</title>
    <style>
      .search-container {
        text-align: center;
        margin-top: 50px;
      }
      .search-box {
        width: 50%;
        padding: 10px;
        font-size: 18px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
      .post-container {
        text-align: center;
        margin-top: 20px;
      }
      .btn-search {
        padding: 10px 15px;
        font-size: 18px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }
    </style>
  </head>
  <body>

    <div class="search-container">
      <%= form_with url: posts_path, method: :get, local: true do %>
        <input type="text" name="search" class="search-box" placeholder="Search posts...">
        <button type="submit" class="btn-search">Search</button>
      <% end %>
    </div>

    <div class="post-container">
      <h1>All Posts</h1>
      <% @posts.each do |post| %>
        <div class="post">
          <h2><%= link_to post.title, post_path(post) %></h2>
          <p><%= post.content.truncate(100) %></p>
        </div>
      <% end %>
    </div>
  </body>
</html>
