<h1><%= t('complete_seller_profile.title') %></h1>

<%= form_for @seller_profile, url: update_account_seller_path(@seller_profile), method: :patch, html: { multipart: true } do |f| %>

  <!-- Displaying Full Name -->
  <div>
    <%= f.label :full_name, t('complete_seller_profile.full_name') %>
    <%= f.text_field :full_name %>
  </div>

  <!-- Displaying Display Name -->
  <div>
    <%= f.label :display_name, t('complete_seller_profile.display_name') %>
    <%= f.text_field :display_name %>
  </div>

  <!-- Displaying Profile Picture -->
  <div>
    <%= f.label :profile_picture, t('complete_seller_profile.profile_picture') %><br>
    <% if @seller_profile.profile_picture.attached? %>
      <%= image_tag url_for(@seller_profile.profile_picture), alt: "Profile Picture", width: 150 %>
    <% else %>
      <p>No Profile Picture</p>
    <% end %>
    <%= f.file_field :profile_picture %>
  </div>

  <!-- Displaying Description -->
  <div>
    <%= f.label :description, t('complete_seller_profile.description') %>
    <%= f.text_area :description %>
  </div>

  <!-- Display Language -->
  <div>
    <%= f.label :language, t('complete_seller_profile.language') %>
    <%= f.select :language, options_for_select(LanguageList::COMMON_LANGUAGES.map { |lang| [lang.name, lang.name] }, @seller_profile.language) %>
  </div>

  <!-- Display Occupation -->
  <div>
    <%= f.label :occupation, t('work_info.occupation') %><br />
    <%= f.text_field :occupation %>
  </div>

  <!-- Display Skills -->
  <div>
    <%= f.label :skills, t('work_info.skills') %><br />
    <%= f.text_area :skills %>
  </div>

  <!-- Display Education -->
  <div>
    <%= f.label :education, t('complete_seller_profile.education') %><br />
    <% if @seller_profile.educations.any? %>
      <% @seller_profile.educations.each do |education| %>
        <div>
          <%= f.fields_for :educations, education do |education_fields| %>
            <%= education_fields.label :degree, "Degree" %>
            <%= education_fields.text_field :degree %>
            <%= education_fields.label :school_name, "School Name" %>
            <%= education_fields.text_field :school_name %>
            <%= education_fields.label :graduation_year, "Graduation Year" %>
            <%= education_fields.number_field :graduation_year %>
          <% end %>
        </div>
      <% end %>
    <% else %>
      <p>No education listed.</p>
    <% end %>
  </div>

  <%= f.submit t('complete_seller_profile.save_profile'), class: 'btn-seller' %>

<% end %>

<!-- Add a button to logout -->
<%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn-logout' %>
